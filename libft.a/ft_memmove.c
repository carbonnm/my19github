#include <string.h>
void *ft_memmove(void *dst, const void *src, size_t n)
{
  char  *dest;
  char  *source;
  int i;

  dest = dst;
  source = src;
  i = 0;
  if (source < dest)
  {
    while ()
  }
  else
  {
    while (len-- > 0)
    {

    }
  }
  return (dest);
}
